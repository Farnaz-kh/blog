import React from "react"

export default (props) =>{
    return(
    <div>
        <h1>{props.text}</h1>
        <h1>{props.title}</h1>
        <h1>{props.titlePage}</h1>
        <h1>{props.titlePost}</h1>
    </div>
    )

}